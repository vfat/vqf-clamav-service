# Data Dictionary — `scan_audit_logs`

## 1. Metadata Table Database

| Table Name | Prefix | Description | Engine |
|---|---|---|---|
| `scan_audit_logs` | `audit` | Menyimpan rekaman audit kepatuhan pemindaian malware (baik via HTTP REST maupun gRPC), hash berkas, ukuran, durasi pemrosesan, dan verdict akhir. | SQLite 3 (WAL Mode) |

---

## 2. Struktur Kolom

### 2.1. scan_audit_logs

| Column Name | Type | Nullable | Default | Keterangan |
|---|---|---|---|---|
| `id` | `TEXT` | NOT NULL | None | Primary Key unik dengan format `audit_<nano_timestamp>` |
| `timestamp` | `DATETIME` | NOT NULL | None | Waktu saat pemindaian selesai dilakukan (format RFC3339 UTC) |
| `consumer_name` | `TEXT` | YES | `NULL` | Nama aplikasi/sistem pemanggil (diambil dari header `X-Consumer-Name` atau gRPC metadata) |
| `client_ip` | `TEXT` | YES | `NULL` | Alamat IP client pemanggil (atau `"grpc"` untuk panggilan gRPC) |
| `file_name` | `TEXT` | YES | `NULL` | Nama asli file yang dipindai (diambil dari multipart, URL basename, atau metadata gRPC) |
| `file_size_bytes` | `INTEGER` | YES | `NULL` | Ukuran file dalam satuan byte |
| `file_sha256` | `TEXT` | NOT NULL | None | Checksum SHA-256 (64 karakter heksadesimal lowercase) |
| `verdict` | `TEXT` | NOT NULL | None | Hasil pemindaian antivirus (`CLEAN` atau `INFECTED`) |
| `virus_name` | `TEXT` | YES | `NULL` | Nama signature virus/malware yang dideteksi oleh ClamAV (hanya terisi jika verdict `INFECTED`) |
| `scan_duration_ms` | `INTEGER` | NOT NULL | None | Durasi waktu pemindaian total dalam milidetik (ms) |
| `quarantine_id` | `TEXT` | YES | `NULL` | Foreign key logis ke `quarantine_records.id` jika file diisolasi ke Vault |

---

## 3. Indexes

### 3.1. scan_audit_logs

| Tipe Index | Nama Index | Target Kolom | Tujuan Kinerja |
|---|---|---|---|
| `PRIMARY KEY` | None (Clustered) | `id` | Integritas unik baris data audit |
| `INDEX` | `idx_audit_timestamp` | `timestamp` | Optimasi query rentang tanggal (`from` / `to`), pagination, dan ekspor data |
| `INDEX` | `idx_audit_verdict` | `verdict` | Optimasi filtering statistik dan metrik alert dashboard |
| `INDEX` | `idx_audit_sha256` | `file_sha256` | Optimasi pencarian riwayat pemindaian berdasarkan hash berkas |

---

## 4. Foreign Keys

### 4.1. scan_audit_logs

| Source Column | Target Table | Target Column | On Delete | On Update | Sifat Relasi |
|---|---|---|---|---|---|
| `quarantine_id` | `quarantine_records` | `id` | `NO ACTION` | `NO ACTION` | **Logical Application Reference** (Divalidasi di layer aplikasi Go) |

---
*Generated by AI Documentor — Database Design Specification Suite*
