# Data Dictionary — `api_keys`

## 1. Metadata Table Database

| Table Name | Prefix | Description | Engine |
|---|---|---|---|
| `api_keys` | `key` | Menyimpan kredensial autentikasi API Key untuk integrasi sistem eksternal, izin akses (*permissions*), status keaktifan, dan waktu pembuatan. | SQLite 3 (WAL Mode) |

---

## 2. Struktur Kolom

### 2.1. api_keys

| Column Name | Type | Nullable | Default | Keterangan |
|---|---|---|---|---|
| `id` | `TEXT` | NOT NULL | None | Primary Key unik identifier record API key |
| `key_hash` | `TEXT` | NOT NULL | None | Hash satu arah dari secret API key (mencegah kebocoran kunci mentah) |
| `name` | `TEXT` | NOT NULL | None | Nama label / identitas consumer aplikasi pemilik key |
| `permissions` | `TEXT` | NOT NULL | None | String permission atau role scope (contoh: `read,scan,admin`) |
| `is_active` | `INTEGER` | NOT NULL | `1` | Flag status keaktifan kunci: `1` (Aktif) atau `0` (Non-aktif/Revoked) |
| `created_at` | `DATETIME` | NOT NULL | None | Waktu saat API Key diterbitkan |

---

## 3. Indexes

### 3.1. api_keys

| Tipe Index | Nama Index | Target Kolom | Tujuan Kinerja |
|---|---|---|---|
| `PRIMARY KEY` | None (Clustered) | `id` | Integritas baris data unik |
| `UNIQUE KEY` | None (Implicit Unique) | `key_hash` | Menjamin satu hash hanya terdaftar sekali dan mempercepat lookup autentikasi |

---

## 4. Foreign Keys

### 4.1. api_keys

| Source Column | Target Table | Target Column | On Delete | On Update | Sifat Relasi |
|---|---|---|---|---|---|
| None | None | None | None | None | Entitas mandiri (*Standalone Authentication Store*) |

---
*Generated by AI Documentor — Database Design Specification Suite*
