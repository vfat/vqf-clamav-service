# Data Dictionary — `system_settings`

## 1. Metadata Table Database

| Table Name | Prefix | Description | Engine |
|---|---|---|---|
| `system_settings` | `set` | Menyimpan konfigurasi operasional dan kredensial administratif persisten (seperti password dashboard Web Admin UI) yang terenkripsi menggunakan cipher AES-256-GCM. | SQLite 3 (WAL Mode) |

---

## 2. Struktur Kolom

### 2.1. system_settings

| Column Name | Type | Nullable | Default | Keterangan |
|---|---|---|---|---|
| `key` | `TEXT` | NOT NULL | None | Primary Key berupa pengenal unik setting (contoh: `ui_password_hash`) |
| `value_encrypted` | `TEXT` | NOT NULL | None | Nilai konfigurasi sensitif yang telah dienkripsi menggunakan AES-256-GCM (Hex/Base64) |
| `updated_at` | `DATETIME` | NOT NULL | None | Waktu saat konfigurasi terakhir diperbarui (format RFC3339 UTC) |

---

## 3. Indexes

### 3.1. system_settings

| Tipe Index | Nama Index | Target Kolom | Tujuan Kinerja |
|---|---|---|---|
| `PRIMARY KEY` | None (Clustered) | `key` | Memastikan lookup instan $O(1)$ berdasarkan kunci setting |

---

## 4. Foreign Keys

### 4.1. system_settings

| Source Column | Target Table | Target Column | On Delete | On Update | Sifat Relasi |
|---|---|---|---|---|---|
| None | None | None | None | None | Entitas konfigurasi mandiri (*Encrypted Key-Value Store*) |

---
*Generated by AI Documentor — Database Design Specification Suite*
