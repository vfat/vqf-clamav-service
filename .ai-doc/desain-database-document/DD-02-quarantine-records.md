# Data Dictionary — `quarantine_records`

## 1. Metadata Table Database

| Table Name | Prefix | Description | Engine |
|---|---|---|---|
| `quarantine_records` | `quar` | Menyimpan metadata dan status siklus hidup berkas terinfeksi yang diisolasi di dalam Quarantine Vault (AES-256-GCM), waktu kedaluwarsa retensi, serta audit proses pemulihan (restore). | SQLite 3 (WAL Mode) |

---

## 2. Struktur Kolom

### 2.1. quarantine_records

| Column Name | Type | Nullable | Default | Keterangan |
|---|---|---|---|---|
| `id` | `TEXT` | NOT NULL | None | Primary Key unik dengan format `Q-<YYYYMMDD>-<UUID8>` (contoh: `Q-20260907-8f2c7a10`) |
| `original_filename` | `TEXT` | NOT NULL | None | Nama asli berkas saat diterima dari client sebelum dienkripsi |
| `file_size_bytes` | `INTEGER` | NOT NULL | None | Ukuran file biner sebelum dienkripsi dalam satuan byte |
| `file_sha256` | `TEXT` | NOT NULL | None | Checksum SHA-256 asli dari file berbahaya (64 karakter hex) |
| `virus_name` | `TEXT` | NOT NULL | None | Nama signature virus/malware yang dideteksi oleh ClamAV engine |
| `source_consumer` | `TEXT` | YES | `NULL` | Identitas sistem consumer pengunggah file berbahaya |
| `stored_path` | `TEXT` | NOT NULL | None | Lokasi file terenkripsi pada filesystem (contoh: `/data/quarantine/Q-20260907-xxxx`) |
| `status` | `TEXT` | NOT NULL | None | Status siklus hidup file: `QUARANTINED`, `RESTORED`, atau `DELETED` |
| `created_at` | `DATETIME` | NOT NULL | None | Waktu saat berkas berhasil dienkripsi dan diisolasi ke Vault |
| `expires_at` | `DATETIME` | NOT NULL | None | Batas waktu kedaluwarsa berkas di karantina sebelum dihapus otomatis |
| `restored_at` | `DATETIME` | YES | `NULL` | Waktu saat berkas dipulihkan oleh Security Admin (jika pernah di-restore) |
| `restored_by` | `TEXT` | YES | `NULL` | Username/identitas admin yang mengotorisasi pemulihan |
| `restore_reason` | `TEXT` | YES | `NULL` | Catatan justifikasi bisnis mengapa berkas di-restore |

---

## 3. Indexes

### 3.1. quarantine_records

| Tipe Index | Nama Index | Target Kolom | Tujuan Kinerja |
|---|---|---|---|
| `PRIMARY KEY` | None (Clustered) | `id` | Integritas unik catatan karantina |
| `INDEX` | `idx_quar_status` | `status` | Mempercepat penyaringan daftar karantina pada dashboard Web Admin UI |
| `INDEX` | `idx_quar_expires` | `expires_at` | Mempercepat background cleaner worker mengecek berkas yang melewati retensi |

---

## 4. Foreign Keys

### 4.1. quarantine_records

| Source Column | Target Table | Target Column | On Delete | On Update | Sifat Relasi |
|---|---|---|---|---|---|
| None | None | None | None | None | Tabel induk (Master Entity karantina) |

---
*Generated by AI Documentor — Database Design Specification Suite*
