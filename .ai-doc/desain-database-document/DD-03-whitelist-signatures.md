# Data Dictionary — `whitelist_signatures`

## 1. Metadata Table Database

| Table Name | Prefix | Description | Engine |
|---|---|---|---|
| `whitelist_signatures` | `white` | Menyimpan daftar hash berkas SHA-256 yang telah diverifikasi sebagai false-positive atau berkas aman resmi, sehingga pemindaian ClamAV socket otomatis di-bypass (Instant Clean). | SQLite 3 (WAL Mode) |

---

## 2. Struktur Kolom

### 2.1. whitelist_signatures

| Column Name | Type | Nullable | Default | Keterangan |
|---|---|---|---|---|
| `sha256_hash` | `TEXT` | NOT NULL | None | Primary Key berupa checksum SHA-256 (64 karakter heksadesimal lowercase) |
| `description` | `TEXT` | YES | `NULL` | Keterangan/alasan mengapa berkas dimasukkan ke dalam daftar putih (whitelist) |
| `added_by` | `TEXT` | YES | `NULL` | Identitas admin/sistem yang menambahkan hash ke daftar putih |
| `created_at` | `DATETIME` | NOT NULL | None | Waktu pencatatan hash ke database whitelist (format RFC3339 UTC) |

---

## 3. Indexes

### 3.1. whitelist_signatures

| Tipe Index | Nama Index | Target Kolom | Tujuan Kinerja |
|---|---|---|---|
| `PRIMARY KEY` | None (Clustered) | `sha256_hash` | Menjamin keunikan hash dan pencarian instant $O(1)$ sebelum mengirim socket streaming |

---

## 4. Foreign Keys

### 4.1. whitelist_signatures

| Source Column | Target Table | Target Column | On Delete | On Update | Sifat Relasi |
|---|---|---|---|---|---|
| None | None | None | None | None | Entitas mandiri (*Standalone Dictionary*) |

---
*Generated by AI Documentor — Database Design Specification Suite*
